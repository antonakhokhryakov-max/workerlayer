# WorkerLayer

Vercel desk is a control surface. Do not add local Workers, startTask execution, or Manifest exec. Proxy those to `WORKERLAYER_HOST_URL` (Mac mini). Stage 2: grant selected, identity-only key, no SSO, no compute picker.
